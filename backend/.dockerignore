# Keep the API image small + CPU-only — exclude everything not needed at runtime.
.git
.venv
venv
__pycache__
*.pyc
.env
.env.*

# Frontend is deployed separately (Vercel)
frontend

# Local data / vector store (state lives in managed services in prod)
data
.chroma

# Docs, samples, agent files
.claude
samples
DOCUMENTATION.md
*.md
!requirements.txt
