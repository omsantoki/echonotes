# Set to the public origin of the FastAPI backend at build time, e.g.
# VITE_API_BASE=https://api.echonotes.example.com
VITE_API_BASE=
# Google sign-in: set to your Web OAuth client id to enable the button (blank hides it).
VITE_GOOGLE_CLIENT_ID=
